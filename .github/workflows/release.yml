# Publie une release à partir d'un tag `vX.Y.Z`.
#
# Le tag est la source de vérité pour la version : elle est réécrite dans
# tauri.conf.json et passée au pack d'effets, de sorte qu'un installeur ne peut
# pas annoncer une version différente de celle qu'il pose dans le magasin de
# pilotes.
#
# Signature : si les secrets sont présents, la DLL et le catalogue sont signés.
# Sinon la release part quand même, mais ses notes disent franchement que le
# paquet n'est pas signé -- Windows refusera d'enregistrer un package pilote
# dont le catalogue ne remonte pas à une autorité de confiance, et il vaut mieux
# l'écrire que laisser quelqu'un le découvrir en l'installant.

name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      version:
        description: "Version x.y.z (sans le v)"
        required: true

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  release:
    runs-on: windows-latest

    # Hissé au niveau du job : une condition `if:` ne voit pas un `env:` déclaré
    # dans l'étape qu'elle garde, et la mienne était donc toujours fausse --
    # l'étape de signature aurait été sautée même avec les secrets en place.
    env:
      SIGNING_PFX: ${{ secrets.SIGNING_PFX }}
      SIGNING_PFX_PASSWORD: ${{ secrets.SIGNING_PFX_PASSWORD }}

    steps:
      - uses: actions/checkout@v4

      - name: Version depuis le tag
        id: version
        shell: pwsh
        run: |
          $version = if ('${{ github.event_name }}' -eq 'workflow_dispatch') {
            '${{ inputs.version }}'
          } else {
            '${{ github.ref_name }}' -replace '^v', ''
          }
          if ($version -notmatch '^\d+\.\d+\.\d+$') {
            throw "Version attendue sous la forme x.y.z, reçu : $version"
          }
          "version=$version" >> $env:GITHUB_OUTPUT
          "Version : $version"

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            dsp -> target
            ui/src-tauri -> target
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ui/package-lock.json

      - name: APO (C++)
        run: |
          cmake -S apo -B apo/build -A x64
          cmake --build apo/build --config Release

      - name: DSP (Rust)
        working-directory: dsp
        run: |
          cargo test --release --locked
          cargo build --release

      - name: Aligner la version sur le tag
        shell: pwsh
        run: |
          $version = '${{ steps.version.outputs.version }}'
          $conf = 'ui/src-tauri/tauri.conf.json'
          $json = Get-Content $conf -Raw
          $json = $json -replace '"version": "[^"]*"', ('"version": "{0}"' -f $version)
          Set-Content $conf $json -Encoding utf8 -NoNewline
          $cargo = 'ui/src-tauri/Cargo.toml'
          (Get-Content $cargo) -replace '^version = ".*"$', ('version = "{0}"' -f $version) |
            Set-Content $cargo -Encoding utf8

      - name: Certificat de signature
        id: cert
        if: env.SIGNING_PFX != ''
        shell: pwsh
        run: |
          $bytes = [Convert]::FromBase64String($env:SIGNING_PFX)
          $path = Join-Path $env:RUNNER_TEMP 'kwiet-signing.pfx'
          [IO.File]::WriteAllBytes($path, $bytes)
          "path=$path" >> $env:GITHUB_OUTPUT

      - name: Pack d'effets
        shell: pwsh
        run: |
          # Table de hachage, pas tableau : le splatting d'un tableau passe les
          # éléments POSITIONNELLEMENT, si bien que -Version arrivait comme
          # valeur de $Version et la version comme valeur de $CertPath.
          $packArgs = @{ Version = '${{ steps.version.outputs.version }}' }
          if ('${{ steps.cert.outputs.path }}') {
            $packArgs.CertPath = '${{ steps.cert.outputs.path }}'
            $packArgs.CertPassword = $env:SIGNING_PFX_PASSWORD
          }
          ./installer/effectpack/build-package.ps1 @packArgs

      - name: Installeur
        working-directory: ui
        run: |
          npm ci
          npm run tauri build

      - name: Empreintes
        shell: pwsh
        run: |
          $dir = 'ui/src-tauri/target/release/bundle/msi'
          Get-ChildItem "$dir/*.msi" | ForEach-Object {
            $hash = (Get-FileHash $_.FullName -Algorithm SHA256).Hash
            "$hash  $($_.Name)"
          } | Set-Content "$dir/SHA256SUMS.txt" -Encoding ascii
          Get-Content "$dir/SHA256SUMS.txt"

      - name: Publier la release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $version = '${{ steps.version.outputs.version }}'
          $dir = 'ui/src-tauri/target/release/bundle/msi'
          $signed = '${{ steps.cert.outputs.path }}' -ne ''
          $notes = @"
          ## Installation

          1. Téléchargez le MSI correspondant à votre langue —
             ``Kwiet_${version}_x64_fr-FR.msi`` ou ``Kwiet_${version}_x64_en-US.msi``
             — et lancez-le.
          2. **Choisissez Kwiet** dans Paramètres → Son → votre micro →
             Améliorations audio. Sans cette étape, Kwiet est installé et inerte :
             le panneau vous le dira.

          Windows 11 24H2 ou plus récent, x64, micro à 48 kHz.
          "@
          if (-not $signed) {
            $notes += @"

          > [!WARNING]
          > **Paquet non signé.** Windows refusera d'enregistrer le pack
          > d'effets : l'application s'installera, le micro ne sera pas traité,
          > et le panneau affichera « Pack d'effets absent ».
          >
          > Rendre cette version installable demande une signature attestation
          > via le Partner Center. Voir le README.
          "@
          }
          $assets = @(Get-ChildItem "$dir/*.msi" | ForEach-Object { $_.FullName })
          $assets += (Resolve-Path "$dir/SHA256SUMS.txt").Path
          gh release create "v$version" @assets `
            --title "Kwiet $version" `
            --notes $notes
